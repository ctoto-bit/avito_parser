[project]
name = "avito-parser"
version = "0.1.0"
description = "Telegram-бот и CLI для поиска арендного жилья на Avito"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
